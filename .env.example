# Environment Variables for Email Configuration

# Email Service Type (smtp or emailjs)
# 'smtp' - Use SMTP for local development
# 'emailjs' - Use EmailJS for production
# If not set, will auto-detect based on NODE_ENV
EMAIL_SERVICE_TYPE=smtp

# SMTP Configuration (for local development)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@hrms-local.com
SMTP_FROM_NAME=HRMS System (Local)

# EmailJS Configuration (for production)
# These are already configured in email-config.ts but can be overridden here
# PUBLIC_EMAILJS_PUBLIC_KEY=LS1lN8SYs5V6vdWUg
# PUBLIC_EMAILJS_SERVICE_ID=service_rnku77s
# PUBLIC_EMAILJS_TEMPLATE_ID=template_komoohv
